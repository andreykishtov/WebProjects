class GameDrawer {
    constructor(canvasID, size = 10) {
        this.size = size;
        this.board = []; // each cell will be here
        //for (let row = 0;.......)
    }

    draw() { /// will bards

    }
}

class CellDrawer {
    constructor(x, y, w, h) {}

    draw() {}
}