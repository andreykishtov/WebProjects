import React from 'react';
// import styled from 'styled-components';



const Day = () => {
    // let day =this.props.day;
    // let taken =this.props.taken;
    // //(days <= 7) ? true : 
    // const Day = styled.div`
    // border: 1px solid black;
    // background-color: ${props => props.taken ? 'grey' : 'white'};
    // `
    // console.log(taken);
    return (
        <div>
            {/* <Day>{day}</Day> */}
        </div>
    );
};

export default Day;